# 代理工作流 —— 思想随笔代理工作流

## 架构

```
Priestess 会话 ──→ 确认方向 → 撰写简报 → tmp/research-brief.md
    │
    ├── 新会话 (Esperanta) ──── 读取 tmp/ → 写作 → output/
    │
    ├── 新会话 (Kaltsit) ──── 审校编排者
    │       │
    │       ├── 第一波：4 个批评者 + 1 个读者（5 个并行）
    │       │       ├── critic-originality（原创性，A1-A4）
    │       │       ├── critic-structure（结构，B1-B12）
    │       │       ├── critic-voice（文风，C1-C8）
    │       │       ├── critic-ai（AI感审查，D1-D8）
    │       │       └── reader-001（感性读者）
    │       │
    │       ├── 第二波：4 个读者（第一波之后）
    │       │       ├── reader-002（理性读者）
    │       │       ├── reader-003（语言读者）
    │       │       ├── reader-004（体验读者）
    │       │       └── reader-005（安全读者）
    │       │
    │       ├── 每个返回摘要行
    │       │       ├── 批评者：<路径> | <通过/拒绝> | 存疑：N | <摘要>
    │       │       └── 读者：<路径> | <id> — <风格> | <印象> | <维度>：<程度>
    │       │
    │       ├── Kaltsit 汇总 → aggregate-report 工具
    │       │       │
    │       │       └── tmp/review-report.md
    │       │              │
    │       │              ├── 通过 → 交付
    │       │              └── 不通过 → tmp/revision-notes.md → 回到 Esperanta 对话修改
    │       │
    │       └── 修改后重新审校（迭代次数不限）
    │
    └── 交付 → 归档 tmp/ + output/ 到 archive/
```

## 上下文传递

`task()` 不携带对话历史。上下文通过文件传递：

- **tmp/research-brief.md**
  - 写入者：Priestess（简报确认后）
  - 读取者：Esperanta

- **tmp/_all-analysis.md**
  - 写入者：`load-references` 工具
  - 读取者：Esperanta
  - 旧版本 @ `archive/YYYY-MM-DD-HHMM/_all-analysis.md`

- **tmp/review-report.md**
  - 写入者：Kaltsit（审校完成后）
  - 读取者：Priestess / Esperanta

- **tmp/revision-notes.md**
  - 写入者：Priestess（用户反馈后）
  - 读取者：Esperanta

`task()` 提示只传递文件路径指针；子代理自己读取内容。

## 角色定义

- **Priestess**
  - 文件：`priestess.md`
  - mode：primary
  - 模型：ds-v4-flash
  - 职责：编排者——调研、确认、引导用户到 Esperanta、协调审校、交付与归档

- **Esperanta**
  - 文件：`esperanta.md`
  - mode：primary
  - 模型：ds-v4-pro（最高）
  - 职责：新会话进行写作/修改

- **Kaltsit**
  - 文件：`kaltsit.md`
  - mode：primary
  - 模型：ds-v4-flash
  - 职责：新会话进行审校——分两波调度 4 个批评者 + 5 个读者

- **critic-originality**
  - 文件：`critic-originality.md`
  - mode：subagent
  - 模型：ds-v4-flash
  - 职责：在 ref/ 中搜索句子/意象/素材相似性（A1-A4）

- **critic-structure**
  - 文件：`critic-structure.md`
  - mode：subagent
  - 模型：ds-v4-flash
  - 职责：检查螺旋结构、金句、隐喻、排比、锚定（B1-B12）

- **critic-voice**
  - 文件：`critic-voice.md`
  - mode：subagent
  - 模型：ds-v4-flash
  - 职责：检查文风、合规性、流程（C1-C8）

- **critic-ai**
  - 文件：`critic-ai.md`
  - mode：subagent
  - 模型：ds-v4-flash
  - 职责：AI 感审查——突发度、句式重复、过渡脚手架、词汇安全区、情绪平坦度、金句设计感、举例通用性、安全区概率（D1-D8）

- **Reader**
  - 文件：`reader.md`
  - mode：subagent
  - 模型：ds-v4-flash
  - 职责：撰写读者印象，返回摘要行

子代理（critic-* + reader）的 `hidden: false` —— 调度对用户可见。

---

## 规则加载

Esperanta、Kaltsit、critic-structure 和 critic-voice 都在前置元数据中包含：

```yaml
prompt: "{file:./.opencode/prompts/common.md}"
```

代理启动时，OpenCode 自动加载 `common.md`（风格定义、核心原则 1-11、金句、快速参考）作为系统提示前缀。无需手动加载。

---

## 数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段 1: 调研与简报                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  网络搜索 ──→ Priestess ──→ 确认方向 ──→ tmp/research-brief.md               │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段 2: 写作（Esperanta 新会话）                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  输入:                                                                      │
│    ├─ tmp/research-brief.md  (调研简报)                                     │
│    ├─ common.md  (自动加载，风格定义)                                        │
│    ├─ tmp/_all-analysis.md  (load-references 工具加载的 6 个分析文件)       │
│    ├─ ref/*/ 原文  (按需读取)                                                │
│    └─ essence()  (高分文章示例)                                              │
│                                                                              │
│  输出:                                                                      │
│    └─ output/{主题}.txt  (草稿，word-count-hook 自动计数)                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段 3: 审校（Kaltsit 新会话）                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  输入: output/{主题}.txt                                                     │
│                                                                              │
│  第一波（5 个并行）:                                                          │
│    ├─ critic-originality  (原创性检查 A1-A4)                                │
│    ├─ critic-structure    (结构检查 B1-B12)                                 │
│    ├─ critic-voice        (文风检查 C1-C8)                                  │
│    ├─ critic-ai           (AI感审查 D1-D8)                                  │
│    └── reader-001          (感性读者)                                        │
│                                                                              │
│  第二波（第一波之后，4 个并行）:                                              │
│    ├─ reader-002          (理性读者)                                        │
│    ├─ reader-003          (语言读者)                                        │
│    ├─ reader-004          (体验读者)                                        │
│    └── reader-005          (安全读者)                                        │
│                                                                              │
│  每个子代理输出: tmp/review-NNN.json / tmp/reader-NNN.json + 摘要行          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段 4: 汇总与判断                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Kaltsit 汇总:                                                               │
│    ├─ 拒绝 > 0 或 存疑 >= 2 → 总体不通过                                    │
│    └── 调用 aggregate-report 工具 → tmp/review-report.md                     │
│                                                                              │
│  判断结果:                                                                  │
│                                                                              │
│    ┌─ 通过 ───────────────────────────────────────────────────────┐         │
│    │                                                              │         │
│    │  终稿 ──→ 阶段 5: 交付 + 归档（回到 Priestess 会话）         │         │
│    │                                                              │         │
│    └──────────────────────────────────────────────────────────────┘         │
│                                                                              │
│    ┌─ 不通过 ───────────────────────────────────────────────────┐         │
│    │                                                              │         │
│    │  用户反馈 → Priestess 写 tmp/revision-notes.md               │         │
│    │                    ↓                                        │         │
│    │  回到 Esperanta 修改 → 回到 Kaltsit 重审            │         │
│    │                                                              │         │
│    └──────────────────────────────────────────────────────────────┘         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 执行流程

### 第一波：调研（Priestess）

打开 Priestess 新会话。Priestess 与用户对话了解主题，通过网络搜索调研，生成结构化简报。

简报确认后，Priestess：
1. 将完整简报写入 `tmp/research-brief.md`
2. 告诉用户打开 Esperanta 新会话

### 第二波：写作（Esperanta）

打开 Esperanta 会话（如从 Priestess 切换过来）。Esperanta 自动：

1. 读取 `tmp/research-brief.md`
2. 调用 `load-references` 工具 → 加载 6 个 analysis.md 文件 → `tmp/_all-analysis.md`
3. 读取 `tmp/_all-analysis.md` 了解所有技巧维度
4. 根据需要读取 ref/ 目录中的原文
5. 调用 `essence()` 学习高分过往文章
6. 写作文章（仅中文，`# 标题`，段落间空行，纯文本）
7. 保存到 `./output/{主题}.txt`（写入时 word-count-hook 自动计数）
8. 自检 6 项（原创性、意象独立性、文风独立性、自然结构、流程合规性、金句位置）

输出：
```
写作完成：output/<文件名>.txt  |  字数：N 字

摘要：<摘要>
金句、好句：<引文>（<位置>）

下一步，请新建 session（Kaltsit），然后输入：
审校 ./output/<文件名>.txt
```

### 第三波：审校（Kaltsit → 4 个批评者 + 5 个读者）

打开 Kaltsit 新会话，输入：

```
审校 ./output/xxx.txt
```

Kaltsit 分两波调度：

**第一波（5 个并行）**：4 个批评者（`critic-originality` + `critic-structure` + `critic-voice` + `critic-ai`）+ 1 个读者（感性）

**第二波（第一波之后）**：4 个读者（理性 + 语言 + 体验 + 安全）

每个写入 `tmp/review-NNN.json` / `tmp/reader-NNN.json` 并返回摘要行。Kaltsit 汇总（拒绝 > 0 或 存疑 >= 2 → 不通过），调用 `aggregate-report` 工具 → `tmp/review-report.md`。

- 全部通过 → 第四波
- 任一不通过 → 用户提供修改反馈 → Priestess 写入 `tmp/revision-notes.md` → 回到 Esperanta 对话修改 → 回到 Kaltsit 对话重审

### 第四波：交付与归档（Priestess）

返回 Priestess 会话。Priestess：

1. 显示终稿摘要 + 路径
2. 请用户确认
3. 调用 `append-metadata` 工具，传入各元数据字段（概要、亮点、方法、主题、扣分理由等）；工具自动提取标题与字数，从 `tmp/review-report.md` 读取综合评分
4. 调用 `archive` 工具归档：创建 `archive/YYYY-MM-DD-HHMM/`，复制终稿 + 全部中间文件，清理 `tmp/`

---

## 读者系统

Kaltsit 从 5 个类别中每个至少选择 1 个读者：

- **感性**
  - 风格：感性读者、怀旧型读者、共情型读者
  - 评估维度：共情程度 / 怀旧强度 / 共情深度

- **理性**
  - 风格：怀疑论者、哲学型读者、实用主义者
  - 评估维度：逻辑漏洞 / 前提审视 / 实用价值

- **语言**
  - 风格：审美型读者、翻译耳读者、节奏型读者
  - 评估维度：语言质感 / 中文自然度 / 节奏感

- **体验**
  - 风格：普通路人、亲历者型、文化比较者
  - 评估维度：留存度 / 真实感 / 文化自觉

- **安全**
  - 风格：锐度审查者、套路督察官、权力审视者、庸俗读者、完整性审查者
  - 评估维度：内容锐度 / 套路密度 / 操控痕迹 / 注意力留存 / 论证完整度

---

## 归档结构

```
archive/
├── 2026-05-13-2257/
│   ├── 都是为了你好_v2.txt       # 终稿（含元数据区块）
│   ├── research-brief.md          # 原始简报
│   ├── review-report.md           # 审校报告
│   ├── review-001.json            # 原创性检查
│   ├── review-002.json            # 结构检查
│   ├── review-003.json            # 文风检查
│   ├── reader-001.json            # 读者 1（感性）
│   ├── reader-002.json            # 读者 2（理性）
│   ├── reader-003.json            # 读者 3（语言）
│   ├── reader-004.json            # 读者 4（体验）
│   ├── reader-005.json            # 读者 5（安全）
│   └── revision-notes.md          # 修改说明（如有）
├── 2026-05-14-2352/
│   └── ...
├── 2026-05-15-1326/
│   └── ...
├── 2026-05-15-2154/
│   └── 月亮不必每夜都圆.txt
│       ...
└── 2026-05-16-1618/
    ├── 词的归处.txt              # 终稿（含元数据区块）
    ├── research-brief.md          # 原始简报
    ├── review-report.md           # 审校报告
    ├── _all-analysis.md           # 六份参考源技法汇总
    ├── review-001.json            # 原创性检查
    ├── review-002.json            # 结构检查
    ├── review-003.json            # 文风检查
    ├── reader-001.json ~ 010.json # 读者感受（两轮 10 份）
    └── revision-notes.md          # 修改说明（如有）
```

`tmp/` 在下一会话启动时清空。`archive/` 永久保存。

### 元数据格式

终稿文件末尾附加标准元数据区块，Priestess 在交付阶段调用 `append-metadata` 工具追加。工具自动提取标题与字数，从 `tmp/review-report.md` 读取综合评分：

```
---
Title: "..."
Score: <0-100>
Reason for deduction: [ "...", "..." ]
Word Count: <N> - Required: <N|Unspec>
Abstract: { ... }
Highlight: [ "...", "...", "..." ]
Approach: { ... }
Topic: { ... }
```

- `Score` — 综合读者评分平均值或审校报告参考分数（自动读取）
- `Reason for deduction` — 扣分理由及违背的原则（由代理参看评审反馈填写）
- `Word Count` — 工具自动统计（排除标题和元数据）
- `Highlight` — 对金句/好句的点评（非摘录），分析作用与位置
- `Approach` — 创作过程、研究路径与写作决策
- `Topic` — 命题原文、分析理解与切入角度

上述元数据由 aggregate-report 工具的 `overall_brief` 字段和读者评分等提供结构支撑。

---

## 关键约束

- **Priestess 只写入 tmp/，不写 output/**
  - 原因：仅调研，不写作

- **Kaltsit + 子代理不写入 output/**
  - 原因：写作与评判分离

- **Esperanta 读取 tmp/ 获取上下文**
  - 原因：`task()` 不携带历史

- **common.md 通过 `prompt` 自动加载**
  - 原因：零手动步骤

- **word-count-hook 在写入时触发**
  - 原因：零手动计数

- **迭代次数不限**
  - 原因：直到用户满意为止

- **子代理调度可见**
  - 原因：用户可观察审校进度

- **归档包含所有中间文件**
  - 原因：完整可追溯

- **`_all-analysis.md` 随其他文件归档**
  - 原因：记录写作时的参考源状态，支持日后回溯

- **终稿元数据由 `append-metadata` 工具自动追加**
  - 原因：统一格式，自动提取标题/字数/评分，禁止手动估算

- **子代理存疑 >= 2 → 总体不通过**
  - 原因：审校纪律

- **批评者使用 `write-critic-report` 工具**
  - 原因：自动编号 + 自动评分

- **critic-ai 使用 `write-critic-report` 工具**
  - 原因：与其他批评者一致的编号和评分逻辑

- **读者使用 `write-reader-report` 工具**
  - 原因：自动编号

---

## 文件参考

- **.opencode/prompts/common.md**
  - 读取者：Esperanta, Kaltsit, critic-structure, critic-voice
  - 用途：风格定义、核心原则（自动加载）

- **esperanta.md 主体**
  - 读取者：Esperanta
  - 用途：写作规则、任务流程、参考、结构

- **kaltsit.md 主体**
  - 读取者：Kaltsit
  - 用途：编排说明

- **critic-originality.md**
  - 读取者：Kaltsit
  - 用途：原创性检查（A1-A4）

- **critic-structure.md**
  - 读取者：Kaltsit
  - 用途：结构与技巧检查（B1-B12）

- **critic-voice.md**
  - 读取者：Kaltsit
  - 用途：文风与合规性检查（C1-C8）

- **critic-ai.md**
  - 读取者：Kaltsit
  - 用途：AI 感审查（D1-D8）

- **reader.md**
  - 读取者：Kaltsit
  - 用途：读者印象（5 个类别）

- **./ref/*/analysis.md**
  - 读取者：全部（通过说明自动加载）
  - 用途：技巧学习

- **./ref/*/ 原文**
  - 读取者：Esperanta, critic-originality
  - 用途：技巧参考 / 搜索比较

- **tmp/research-brief.md**
  - 读取者：Esperanta
  - 用途：上下文传递

- **tmp/_all-analysis.md**
  - 读取者：Esperanta
  - 用途：参考技巧汇总

- **tmp/review-report.md**
  - 读取者：Priestess
  - 用途：审校结果传递

- **tmp/revision-notes.md**
  - 读取者：Esperanta
  - 用途：修改要求

- **aggregate-report 工具**
  - 使用者：Kaltsit
  - 用途：合并批评报告 + 读者反馈

- **append-metadata 工具**
  - 使用者：Priestess
  - 用途：在文章末尾追加标准元数据区块

- **archive 工具**
  - 使用者：Priestess
  - 用途：归档终稿 + 全部中间文件，清理 tmp/

- **essence 工具**
  - 使用者：Esperanta
  - 用途：高分文章示例（分数 > 80）

- **count 工具**
  - 使用者：Priestess
  - 用途：中文字数统计

- **load-references 工具**
  - 使用者：Esperanta
  - 用途：加载全部 6 个参考分析文件

- **write-critic-report 工具**
  - 使用者：critic-*
  - 用途：撰写批评审校报告（自动编号 + 计算分数）

- **write-reader-report 工具**
  - 使用者：Reader
  - 用途：撰写读者印象（自动编号）

- **word-count-hook 插件**
  - 使用者：系统
  - 用途：写入 output/ 时自动计数

- **exa MCP**
  - 使用者：Priestess
  - 用途：网络深度搜索

- **archive/**
  - 用途：历史记录

---

## 审校清单

### 原创性（critic-originality）

- **A1：从 ref/ 复制句子结构（包括改写）**
  - 严重倍率：5
  - 拒绝条件：≥1 项发现

- **A2：来自个人经验/独立调研的素材**
  - 严重倍率：5
  - 拒绝条件：≥1 项发现

- **A3：与 ref/ 具有相同论证功能的意象/隐喻**
  - 严重倍率：3
  - 拒绝条件：≥2 项发现

- **A4：结尾句子结构与 ref/ 相似**
  - 严重倍率：5
  - 拒绝条件：≥1 项发现

存疑项：每个 -5。分数 = max(0, 100 - 总扣分)。任一严重倍率≥4项触发或总分 ≥ 40 则拒绝。

### 结构与技巧（critic-structure）

- **B1：议论文检测**
  - 严重倍率：4
  - 拒绝条件：拒绝

- **B2：螺旋自然度**
  - 严重倍率：4
  - 拒绝条件：拒绝

- **B3：金句存在性**
  - 严重倍率：1
  - 拒绝条件：拒绝

- **B4：金句质量**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **B5：隐喻一致性**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **B6：功能排比**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **B7：维度锚定**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **B8：感官场景使用**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **B9：矛盾保留**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **B10：结尾开放性**
  - 严重倍率：4
  - 拒绝条件：拒绝

- **B11：角色检查**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **B12：认知检查**
  - 严重倍率：4
  - 拒绝条件：拒绝

存疑项：每个 -5。分数 = max(0, 100 - 总扣分)。B1/B2/B10/B12 触发或总分 ≥ 40 则拒绝。

### 文风与合规性（critic-voice）

- **C1：社群声音（"我们" vs "你"）**
  - 严重倍率：4
  - 拒绝条件：拒绝

- **C2：感叹号使用**
  - 严重倍率：1
  - 拒绝条件：拒绝

- **C3：为读者下结论**
  - 严重倍率：4
  - 拒绝条件：拒绝

- **C4：学术风格引用**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **C5：励志语气**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **C6：说教框架**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **C7：语言合规性（仅中文）**
  - 严重倍率：1
  - 拒绝条件：拒绝

- **C8：体裁一致性**
  - 严重倍率：3
  - 拒绝条件：拒绝

存疑项：每个 -5。分数 = max(0, 100 - 总扣分)。C1/C3 触发或总分 ≥ 40 则拒绝。

### AI 感审查（critic-ai）

- **D1：突发度（Burstiness）**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **D2：句式重复**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **D3：过渡脚手架**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **D4：词汇安全区概率**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **D5：情绪平坦度**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **D6：金句设计感**
  - 严重倍率：2
  - 拒绝条件：拒绝

- **D7：举例通用性**
  - 严重倍率：3
  - 拒绝条件：拒绝

- **D8：安全区重复使用**
  - 严重倍率：2
  - 拒绝条件：拒绝

存疑项：每个 -5。分数 = max(0, 100 - 总扣分)。D1/D3/D5 触发或总分 ≥ 40 则拒绝。

> 详细评分算法与综合评分方法参见 [SCORING.md](./SCORING.md)

---

## 危险信号（任一触发即拒绝）

- **感叹号作为情感拐杖**
  - 快速检查：总数 > 4 且没有它们句子显得虚弱

- **隐喻堆砌**
  - 快速检查：200 字内有 2+ 个不同域的核心隐喻

- **强行励志**
  - 快速检查：语气是"我们终将胜利"而非"我们共同面对"

- **告诉读者该感受什么**
  - 快速检查："你应该感到……"、"你一定会……"

- **为读者下结论**
  - 快速检查：矛盾之后紧接着"答案是……"

- **参考来源的例子**
  - 快速检查：例子出现在 ref/ 中

- **相似句子/结尾**
  - 快速检查：句子骨架或结尾与 ref/ 匹配

- **缺少 analysis.md 技巧**
  - 快速检查：没有参考来源技巧的痕迹

- **无可引用句子**
  - 快速检查：移除任何句子都不影响论证

- **移除标题失去主题**
  - 快速检查：没有标题猜不出主题

- **说教框架**
  - 快速检查："我们必须承认……"、"值得注意的是……"用于组织段落

- **证明论点**
  - 快速检查：结构是"数据 → 结论 → 建议"而非螺旋式
